# This file intentionally contains Python lint violations.
# It is used by test_ruff_catches_known_bad_python to verify
# that the ruff configuration correctly detects errors.
# DO NOT fix the violations below.

import os
import sys

x = 1
